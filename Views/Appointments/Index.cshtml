@model List<Appointments>

<h2>Appointmentss</h2>

<table class="table">
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <!-- Add more columns if needed -->
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var patient in Model)
        {
            <tr>
                <td>@patient.FirstName</td>
                <td>@patient.LastName</td>
                <!-- Add more columns if needed -->
                <td>
                    <a asp-controller="Appointments" asp-action="Details" asp-route-id="@patient.AppointmentsId">Details</a> |
                    <a asp-controller="Appointments" asp-action="Edit" asp-route-id="@patient.AppointmentsId">Edit</a> |
                    <a asp-controller="Appointments" asp-action="Delete" asp-route-id="@patient.AppointmentsId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
