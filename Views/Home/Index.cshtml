@model List<Patient>

<h2>Patients</h2>

<table class="table">
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <!-- Add more columns if needed -->
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var patient in Model)
        {
            <tr>
                <td>@patient.FirstName</td>
                <td>@patient.LastName</td>
                <!-- Add more columns if needed -->
                <td>
                    <a asp-controller="Patient" asp-action="Details" asp-route-id="@patient.PatientId">Details</a> |
                    <a asp-controller="Patient" asp-action="Edit" asp-route-id="@patient.PatientId">Edit</a> |
                    <a asp-controller="Patient" asp-action="Delete" asp-route-id="@patient.PatientId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
